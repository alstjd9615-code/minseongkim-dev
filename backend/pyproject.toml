[tool.pytest.ini_options]
# Run from the backend/ directory: `pytest` or `python -m pytest`
testpaths = ["eval/tests"]
pythonpath = ["."]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short"
